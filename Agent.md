# Agent 工作规则

## 提交 / 推送规则（强制，2026-09-01 起）

1. **任何 `git commit` / `git push` 之前，必须先征得用户明确同意。**
2. 完成任务、写完代码、改完文档后，**不得直接提交**。
3. 提交前必须先在回复中向用户展示：
   - 将提交的文件清单（`git status` / `git diff --stat`）
   - 拟使用的 commit message
   - 是否需要推送、推到哪个分支
   等用户确认后，再执行 commit / push。
4. 除非用户明确要求，禁止执行以下操作：
   - `git commit --amend`（改写已有提交）
   - `git push --force` / `git push --force-with-lease`（强推覆盖远程）
   - 任何重写历史（rebase / reset / filter-branch 等）的操作
5. 若用户未明确说"提交"，则默认**只改文件、不提交**。

## 其他约定

- 修改源码/配置文件前，先读取文件确认当前内容，避免基于过期内容做编辑。
- 构建、打包、推送等耗时操作，先说明计划再执行。
- 发现用户代码与文档描述不一致时，以代码实际行为为准并提醒用户。
